[defaults]
inventory = hosts
vault_password_file = pass.sh
# NOTE: `ansible_python_interpreter` is a host/inventory var, not an ansible.cfg key --
# setting it here was silently ignored. `interpreter_python` is the config equivalent.
interpreter_python = auto_silent
timeout = 30
executable = /bin/bash

[ssh_connection]
pipelining = True
