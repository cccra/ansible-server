---
hook: true

containers:
  - proxied: true
    image: lscr.io/linuxserver/lidarr:latest
    networks:
      - name: media_network
    dns_servers:
      - 1.1.1.1
      - 8.8.8.8
    volumes:
      - "{{ docker_dir }}/lidarr:/config"
      - "{{ mergerfs_root }}:/tank"
