---
docker_deps:
  - 'apt-transport-https'
  - 'ca-certificates'
  - 'curl'
  - 'gnupg'
  - 'python3-pip'

# Plain Debian installs Docker from the distro repos (the docker-ce apt repo is
# only wired up for Ubuntu above).
docker_packages:
  - 'docker.io'
  - 'containerd'
