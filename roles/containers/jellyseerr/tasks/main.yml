---
- name: Make sure the seerr container is created and running
  community.docker.docker_container:
    name: "jellyseerr"
    image: "ghcr.io/seerr-team/seerr:latest"
    labels: {"nas-setup.service": "jellyseerr"}
    pull: "{{ container_pull }}"
    networks:
      - name: media_network
    # Networks are declarative here: the default allow_more_present would connect
    # the new network but leave the container attached to the old one.
    comparisons:
      networks: strict
    dns_servers:
      - 1.1.1.1
      - 8.8.8.8
    state: 'started'
    env:
      "PUID": "{{ guid }}"
      "PGID": "{{ guid }}"
      "TZ": "{{ timezone }}"
    volumes:
      - "/etc/localtime:/etc/localtime"
      - "{{ docker_dir }}/seerr:/app/config"
    restart_policy: unless-stopped
