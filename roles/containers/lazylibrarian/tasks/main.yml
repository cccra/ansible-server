---
- name: Make sure the lazylibrarian container is created and running
  community.docker.docker_container:
    name: "lazylibrarian"
    image: "ghcr.io/linuxserver/lazylibrarian:latest"
    labels: {"nas-setup.service": "lazylibrarian"}
    pull: "{{ container_pull }}"
    networks:
      - name: media_network
    # Networks are declarative here: the default allow_more_present would connect
    # the new network but leave the container attached to the old one.
    comparisons:
      networks: strict
    state: 'started'
    env:
      "PUID": "{{ guid }}"
      "PGID": "{{ guid }}"
      "TZ": "{{ timezone }}"
    volumes:
      - "/etc/localtime:/etc/localtime:ro"
      - "{{ docker_dir }}/lazylibrarian:/config"
      - "{{ mergerfs_root }}:/tank"
    restart_policy: unless-stopped
